"use client";

import SandboxEngine from '@/components/SandboxEngine';

export default function MockH5PContent() {
  return (
    <div className="min-h-screen bg-gray-50 flex flex-col items-center justify-center p-4 font-sans">
      <SandboxEngine />
    </div>
  );
}
